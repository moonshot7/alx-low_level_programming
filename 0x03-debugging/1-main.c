#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * This function is the entry point for the program. It prints a message
 * to the console and returns 0.
 *
 * (no parameters needed for this function)
 * Member '\\o/\n': Represents a visual emoticon of joy and celebration.
 * Return: 0 on success
 */

int main(void)
{
int i;

printf("Infinite loop incoming :(\n");
/**
* i = 0;

* while (i < 10)
* {
* putchar(i);
* }
*/
printf("Infinite loop avoided! \\o/\n");
return (0);
}
